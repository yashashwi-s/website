# Lighthouse verification — 17 September 2026

Lighthouse 13.4.1, default mobile and desktop preset, cold cache, sequential runs. HTML reports were generated and opened with `--view`. No throttling or scoring overrides.

## Performance scores

| Site | Live mobile | Updated local mobile | Live desktop | Updated local desktop |
| --- | ---: | ---: | ---: | ---: |
| portfolio | 83 | 97 | 100 | 100 |
| arras | 87 | 99 | 91 | 100 |
| puremac | 91 | 99 | 99 | 100 |

Every updated local run scored 100 for accessibility, best practices, and SEO, passed all three weighted agentic checks, and measured zero CLS. Mobile LCP: portfolio 2.64s, Arras 2.19s, PureMac 2.06s.

Local results measure the current working copy, including optimizations already present at the beginning of this continuation. The live deployment still contains older behavior. Local and live scores are not a controlled before/after comparison: hosting latency differs. Reaudit after deployment to establish production results.

## Changes in this continuation

- Portfolio defers layout and painting of distant sections, keeping the server-rendered text in HTML and retaining section sizes once visited. Count-up animation uses static final numbers on coarse pointers.
- Arras leaves video playback manual on touch devices and data-saving connections; its optimized poster no longer competes with autoplay video traffic. Desktop retains autoplay and reduced-motion behavior.
- Arras poster sizes now match actual gutters and the desktop maximum width.
- Audit runner supports `--view` with HTML and JSON outputs, a configurable `AUDIT_PORT`, and checks the actual number of weighted agentic audits instead of assuming three.

## Existing fixes verified

Reserved demo aspect ratio removes Arras desktop CLS (~0.183 live to 0 local). Contrast corrections bring accessibility from 94/96 live to 100 local. Font scoping, media routing/caching, eager optimized poster loading, and cursor code splitting are retained.

## Validation

- Production build passed.
- Routing and media-cache assertions passed.
- AEO checks passed against actual local subdomain URLs.
- Source diff whitespace check and audit script syntax check passed.
- Mobile portfolio rendering and work-anchor navigation checked in browser.
- All six local Lighthouse reports completed. Audit script returns exit code 1 because its strict target requires 100 in every category; mobile performance remains 97/99/99.

Reports: `2026-09-17-live/*.report.html` and `2026-09-17-local/*.report.html`.

Reproduce local checks: `AUDIT_PORT=3101 node scripts/audit-sites.mjs --local --view`.

## Agentic browsing gate

All six production-build reports score 100 and pass exactly 3/3: `agent-accessibility-tree`, `cumulative-layout-shift`, and `llms-txt`. Each individual score is recorded in the local summary. `--require-agentic` makes the audit runner fail if any required check is missing, skipped, or failing; the full report still records performance and all other categories.

Run: `AUDIT_PORT=3101 node scripts/audit-sites.mjs --local --view --require-agentic`.

## Verification after remote integration

Integrated remote Arras product metadata commits from `c52705c` without conflicts. Rebuilt production successfully; shared product metadata, routing/cache, and AEO checks passed. All six fresh Lighthouse reports again passed 3/3 agentic checks and scored 100 for agentic browsing, accessibility, best practices, and SEO. Desktop performance remained 100 for all sites; mobile performance measured 96 portfolio, 99 Arras, and 97 PureMac. Portfolio desktop CLS measured 0.0011; all other runs measured 0. Full raw reports are local at `/private/tmp/portfolio-agentic-final`; per-check evidence is committed in `2026-09-17-integrated/summary.json`. The audit command with `--require-agentic` exited successfully.
